# Revenue Ops

Status: partial until RevenueCat, store products, entitlements, and sandbox proof are recorded.

## Definition Of Good

### PRESENT — artifact exists (required before any revenue claim)

- `revenue/REVENUE_OPS.md` (this file) documents the chosen paywall model, product mix, entitlement name, and offering ID.
- Every App Store and/or Google Play product is listed with its store product ID, RevenueCat product ID, RevenueCat product type, entitlement membership, and package ID.
- A `currentOffering` is identified by name (default: `default`) and every package it contains is listed.
- `operations/PROVIDER_PROOF.md` contains a RevenueCat row with an `evidence path` referencing a proof artifact (e.g. `revenue/revenuecat-proof.md`).
- Paywall model decision is recorded: hard paywall, soft paywall/freemium, reverse trial, or web funnel, with the value moment shown before the paywall.
- Trial/intro-offer duration, plan mix (weekly/monthly/annual/lifetime), and price are recorded as explicit decisions.

### PROVEN — hard errors when revenue lane is "done"

All items below are hard errors (exit 1) when `lanes.revenue.status` is `"done"`:

1. **Founder-gated live probe artifact (`revenue/revenuecat-proof.json`) exists on disk.**
   The file must be generated by running:
   ```
   doppler run -- npm run probe:revenuecat
   ```
   It must carry a `"probe":"revenuecat@1"` fingerprint, a fresh ISO timestamp
   (warn if older than 30 days), a non-empty `offering_id`, and at least one
   `entitlement_id`. A hand-typed copy of the example file fails the Tier-1
   content floor (byte-identity check). Prose/keyword scans of a markdown file
   are no longer accepted as the primary offering check when the proof JSON is
   present; the JSON artifact is the authoritative proof.

2. **Entitlement unlock confirmed.** The proof artifact or `operations/PROVIDER_PROOF.md`
   entry must record that a sandbox purchase granted the named entitlement
   inside the app — not just that the purchase appeared in the RevenueCat
   dashboard. Record this in `revenue/revenuecat-proof.md` (the human-readable
   companion stub).

3. **No MISSING_METADATA.** No App Store subscription product listed in
   `revenue/REVENUE_OPS.md` may be in `MISSING_METADATA` state. Subscription-group
   localizations must be confirmed created before the lane is marked done.

4. **Product-type reconciliation.** Every product listed in `revenue/REVENUE_OPS.md`
   must declare its RevenueCat product type, and that type must match the App
   Store/Google Play product type: `non_consumable` for lifetime/one-time
   unlocks; `auto_renewable_subscription` for recurring subscriptions. A
   lifetime product mapped as `non_renewing_subscription` is a hard error.

5. **Restore tested.** The proof artifact or `engineering/PRODUCTION_READINESS.md` must
   record that the restore-purchases path was exercised without granting a
   duplicate entitlement.

### OPTIMIZED — warnings (taste stays human; logged override accepted)

- Paywall model, trial duration, and price are documented with a benchmark citation (e.g. RevenueCat State of Subscription Apps 2026) explaining why this combination was chosen rather than a default. Absence is a warning, not an error.
- Annual plan is offered as the highlighted option alongside monthly/entry; leading with monthly only triggers a warning.
- Hard paywall or value-moment decision is recorded with a rationale; choosing freemium without documenting the word-of-mouth / network-effect justification triggers a warning.
- Involuntary-churn recovery (grace period, billing-issue webhook → dunning) is addressed in the engineering plan or noted as deferred with a reason.

## Contract

| Item | Expected proof | Artifact |
| --- | --- | --- |
| Entitlement | sandbox purchase grants access in app (not only dashboard) | revenue/revenuecat-proof.json (probe artifact) + revenue/revenuecat-proof.md (manual notes) |
| Offering | `currentOffering` resolves non-empty in Release build | revenue/revenuecat-proof.json `offering_id` + `offering_resolved: true` |
| Product types | each product type matches store + RC dashboard (lifetime = non_consumable) | revenue/REVENUE_OPS.md product table |
| MISSING_METADATA | all products cleared before lane done | revenue/REVENUE_OPS.md |
| Restore | restore path tested without duplicate entitlement | engineering/PRODUCTION_READINESS.md |
| Paywall model | hard/soft/freemium/reverse-trial decision recorded with rationale | revenue/REVENUE_OPS.md |
| Trial/price | explicit recorded decision citing benchmarks | revenue/REVENUE_OPS.md |

## Products And Offering

<!-- Fill in before marking revenue lane done. -->

| Store Product ID | RC Product ID | RC Product Type | Entitlement | Package ID | MISSING_METADATA cleared? |
| --- | --- | --- | --- | --- | --- |
| _example: com.app.premium.monthly_ | _example: premium_monthly_ | _auto_renewable_subscription_ | _premium_ | _$rc_monthly_ | _yes/no_ |

Current offering ID: `default`

RevenueCat proof artifact path: `revenue/revenuecat-proof.json` <!-- machine-generated by probe:revenuecat; must exist on disk before done -->

## Paywall Model Decision

<!-- Required. Record the chosen model and the rationale. -->

Model: <!-- hard_paywall | freemium | reverse_trial | web_funnel -->

Value moment shown before paywall: <!-- personalized plan | analysis preview | completed setup | none -->

Rationale: <!-- cite benchmarks or product reasons -->

## Trial And Pricing Decision

<!-- Required. Record explicit choices, not defaults. -->

Trial duration: <!-- e.g. 7 days, none -->

Plan mix: <!-- monthly | annual | lifetime | monthly+annual | other -->

Highlighted plan: <!-- annual | monthly -->

Price (annual): <!-- e.g. $49.99/year -->

Price (monthly): <!-- e.g. $9.99/month -->

Benchmark cited: <!-- e.g. RevenueCat State of Subscription Apps 2026 §4c -->

### Competitor Anchor

<!-- 5-10 direct competitors from strategy/RESEARCH.md, per paywall-pricing-and-experiments.md §3.
     Prices from AppKittie IAP data plus the live store listing, with the date checked. -->

| Competitor | Store | Monthly | Annual | Checked |
| --- | --- | --- | --- | --- |
| _example: CompetitorName_ | _example: iOS_ | _example: $9.99_ | _example: $59.99_ | _example: YYYY-MM-DD_ |

Category range and median: <!-- e.g. $4.99-$14.99 monthly, median $9.99 -->

Candidate points considered: <!-- 2-3 candidates and the one-line rationale for each -->

Founder approved: <!-- YYYY-MM-DD — pricing is founder-only (revenue-monetization.md §2) -->

### Cancellation-Reason Mix

Aggregated from `cancellation_reason_selected` (taxonomy in `billing-health-and-reactivation.md` §3);
reviewed at each kill-or-scale checkpoint — a too_expensive majority is pricing evidence.

### Billing And Reactivation Events

The revenue lifecycle events this document depends on (`billing-health-and-reactivation.md` §2/§3).
Every name below must stay in the `analytics/ANALYTICS.md` Event Contract — `check:analytics-catalog`
reconciles this list against the catalog, so a rename or removal fails instead of drifting:

- `billing_issue_detected`
- `billing_recovery_prompt_shown`
- `payment_method_update_started`
- `billing_recovered`
- `billing_issue_churned`
- `cancellation_reason_selected`
- `subscription_paused`
- `reactivation_offer_shown`
- `reactivated`

| Period | too_expensive | missing_feature | found_alternative | not_needed | technical_issue | other |
| --- | --- | --- | --- | --- | --- | --- |

## Economics Snapshot

<!-- Pulled from RevenueCat charts per the Economics Pull recipe
     (tool-recipes/revenue-email-analytics.md) before each kill-or-scale checkpoint.
     Record the docs basis and chart names used alongside the numbers. -->

| Date | MRR | Realized LTV (paying) | Download-to-paid | Churn (voluntary / involuntary) | Source |
| --- | --- | --- | --- | --- | --- |

## Paywall Experiment Backlog

The first paywall is a hypothesis. This backlog is the standing program that iterates it (`paywall-pricing-and-experiments.md` §4): one experiment at a time per surface, judged on cohort economics over at least one renewal-decision window, price/offer changes founder-approved. `check:revenue` requires current activity once the app has been live four weeks with this lane done: an active row, a completed row started within the last eight weeks, or a planned row dated to start within 60 days.

| Started | Hypothesis | Variant | Primary metric | Status (planned / active / completed) | Result / decision |
| --- | --- | --- | --- | --- | --- |

## Founder-Gated Probe Step

The revenue lane requires a live RevenueCat API probe to confirm offerings and
entitlements resolve correctly. This probe is founder-gated because it requires
live credentials injected via Doppler.

### Running The Probe

```
doppler run -- npm run probe:revenuecat [-- --root /path/to/business-repo]
```

Required Doppler secrets:
- `REVENUECAT_SECRET_API_KEY` — RevenueCat secret key (starts with `sk_`)
- `REVENUECAT_PROJECT_ID` — RevenueCat project ID (visible in dashboard URL)

### What The Probe Does

1. Calls `GET /v2/projects/{project_id}/offerings` and records the default
   offering ID and package IDs.
2. Calls `GET /v2/projects/{project_id}/entitlements` and records entitlement IDs.
3. Writes a non-secret proof artifact at `revenue/revenuecat-proof.json` with a
   `"probe":"revenuecat@1"` fingerprint, HTTP statuses, offering ID, package IDs,
   entitlement IDs, and an ISO timestamp.

No API key or secret is written to the artifact.

### After The Probe

1. Review `revenue/revenuecat-proof.json` to confirm `offering_resolved: true`
   and `entitlements_present: true`.
2. Open `revenue/revenuecat-proof.md` and fill in the sandbox purchase, entitlement
   grant, restore, and Release-build evidence sections.
3. The validator (`npm run check:revenue`) will verify the JSON artifact's shape,
   fingerprint, timestamp freshness, and non-empty offering/entitlement values.
   The validator also applies a Tier-1 content floor: if the JSON is byte-identical
   to the example file or below a minimum size threshold, it is rejected.
