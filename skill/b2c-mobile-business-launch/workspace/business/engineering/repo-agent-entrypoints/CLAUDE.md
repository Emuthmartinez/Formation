# CLAUDE.md

Read `AGENTS.md` first; it is the canonical operating guide for {{APP_NAME}}.

Claude-specific notes:

- Session Continuity: At the start of every new session, resume, status check, or handoff, reconstruct state from `AGENTS.md`, `state/PROJECT_STATE.yaml`, `state/launch-cockpit.html`, `operations/BUSINESS_ACCESS.md`, `operations/business-access.json`, `operations/AGENT_OPERATIONS.md`, `operations/agent-operations.json`, `operations/ORCHESTRATION.md`, `engineering/PRODUCTION_READINESS.md`, `operations/FAILURE_CARDS.md`, and `git status --short`. Do not rely on prior chat context as source truth.
- Assume beginner founder knowledge and act as the business operator: name the current phase/outcome, define unfamiliar terms, and present each gate through AskUserQuestion with two or three choices (plain text if unavailable), one recommendation, consequences, and a safe skip/fallback/defer route. Protected gates only defer; silence is not consent. Keep `operations/BUSINESS_ACCESS.md` plus its gate lifecycle current instead of returning a checklist.
- Continue using the `b2c-mobile-business-launch` skill for launch, revenue, analytics, paid UA, store, security, growth, and production-readiness work. Do not ask the founder to say "use the skill" again once the lane is active.
- For design work, use the Design Room state loop: mutate `studio/seed/business.json` and `studio/seed/theme.tokens.json`, render `design/design-room.html`, and version/baseline with git instead of creating one-off design proposal files.
- Use `APP_AGENTS.md` and `agents/` for role routing, but keep product truth in `AGENTS.md` and the source docs it names. For broad work, use subagents or record why they are unavailable or unsafe.
- Use Compound Engineering skills when available for `ce-update`, brainstorm, plan, work, worktree, review, test, and proof stages; record unavailable routes rather than skipping them silently.
- Runtime split (recommended bias, not a requirement): Claude is the better fit for the pre-build stages through the spec (research, 11-star, design, growth, analytics, spec/handoff); Codex is the better fit for the core app build. For long-running, massively parallel, or adversarial pre-build stages use a Dynamic Workflow (`ultracode`, `/effort ultracode`, or `/deep-research`) — set a token budget, pair loops with `/goal`, quarantine untrusted reviews/social/scraped input, and keep producer and verifier agents separate. Don't waste a Claude workflow on the build Codex is better at; fall back to subagents and record the reason when workflows are unavailable. Record which runtime handled which lane in `operations/ORCHESTRATION.md` and `state/PROJECT_STATE.yaml`.
- Use subagents only for scoped independent audits or isolated file ownership recorded in `operations/ORCHESTRATION.md`.
- Use the launch skill's `frontier-agent-operations.md` before authenticated browser/account/social/native work; access is not blanket permission, and mutations need exact-target scope, approval, read-back proof, and state reconciliation.
- Use ASC CLI/skills for App Store Connect work by default, including app creation, metadata, screenshots, TestFlight, review/status, products/subscriptions, and RevenueCat catalog sync. Manual console-only instructions are a fallback after CLI/auth/capability blockers or founder approval.
- For native iOS proof, use `AGENTS.md` plus the launch skill's `xcodebuildmcp-testing.md` Route Ladder. Rung 0 is the in-app iOS Simulator and is the default first route in a local Claude Code Desktop session — no install, no account; ask for the outcome ("run the app in the simulator and tap through signup") and the pane opens itself. It is unavailable in cloud and SSH sessions. Codex Desktop native iOS/XcodeBuildMCP tools stay first-class when exposed; CLI users can enable the built-in `computer-use` MCP server, or use SnapshotPreviews for preview-only PNG/JSON proof and serve-sim for browser-visible simulator proof. Record the rung, the fixture-account decision, and the limitations in `engineering/PRODUCTION_READINESS.md` and `strategy/TOOL_DECISIONS.md`.
- Do not stage, commit, push, release, submit builds, connect accounts, spend money, change pricing, mutate credentials, or mark readiness from subagent findings alone.
- Refresh official docs and local CLI help before changing third-party command guidance for Doppler, PostHog, RevenueCat, Stripe, Resend, Apple/App Store Connect, Google Play, XcodeBuildMCP, MobAI, Refero, Higgsfield, Fastlane, Remotion, or similar fast-moving tools.
