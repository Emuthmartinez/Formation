{
  "skill": "b2c-mobile-business-launch",
  "version": "0.49.0",
  "updatedAt": "2026-07-29",
  "sourcePath": "skill/b2c-mobile-business-launch",
  "releaseNotes": [
    "The app-copy contract: every word a user reads is authored before it is typed. New COPY_DECK.md artifact carries every user-facing string screen by screen (onboarding, paywall, core loop, empty states, errors, permission primes, settings, dialogs) with lowercase dot-namespaced keys that ship unchanged into the string resources, and the previously-orphaned COPY_BRIEF.md finally gets its shipped template. references/conversion-copy.md grows four sections \u2014 The Copy Deck, App Surface Recipes (12 formulas mined from cited 60fps.design shots of premium consumer apps), Banned In App Copy (machine-parsed rule lists, same doc-is-the-gate contract as no-slop), and Localization Readiness (per-stack string externalization from the first commit: .xcstrings, i18next + expo-localization, ARB + gen-l10n, next-intl; ICU plurals, no concatenation, locale-formatted prices, pseudo-loc pass, string freeze). The deck is wired into the build path the way COPY_BRIEF never was: onboarding-conversion.md, engineering-orchestration.md AGENTS source-of-truth list and plan requirements, ONBOARDING.md screen-sequence Copy column (deck keys replace the old Product-specific placeholders), TECH_SPEC.md Strings And Localization Readiness, ENGINEERING_PLAN.md, BRAND.md cross-surface checklist, and the design lane evidence in PROJECT_STATE.yaml.",
    "check:app-copy is the new deterministic gate (66 -> 67 checks): parses its banned-vocabulary and placeholder-shape lists out of conversion-copy.md, scans authored deck cells for internal vocabulary (with a deck-local Allowed terms allowlist mirroring founder-copy), placeholder filler, raw snake_case/SCREAMING_SNAKE identifiers (ICU interpolations exempt), malformed keys, and locale tiers; scans the ONBOARDING.md Copy column (the original leak site); requires TECH_SPEC.md to name the string-externalization mechanism once engineering is done; warns (never errors) on mixed control-label case because Apple and Material genuinely disagree; and downgrades every business-repo finding to a warning for live apps (product.phase phase_6*) so shipped businesses get a tracked backfill instead of a broken build. All four archetype starters stop leaking maintainer copy: the Starter/scaffold/prompt-pack landing text is replaced with human example copy consumed from a new lib/strings.ts externalization seed, under fictional gate-detectable brands (Fernpath, Wrenfeed, Loomroom, Glimmerjar) whose survival in a real launch fails the gate. Nine validator fixtures, three LaunchBench scenarios (app-ui-copy-reads-like-spec, copy-deck-skipped-during-build, app-strings-hardcoded-not-localization-ready), five source-registry entries, and the SKILL.md lane row rewrite (Conversion copy -> Copy + app strings) paid for by subtraction at 46,079 of 46,080 bytes.",
    "Codex review round 1 hardening (5 threads): ONBOARDING key-prefix references are reconciled against authored deck keys (app_copy.deck_coverage_missing -- a one-row deck wearing authored status fails); duplicate deck keys fail (string resources keep one value per key); the ONBOARDING Copy column gets the same banned-vocabulary scan as deck cells with backticked key spans stripped; TECH_SPEC untouched externalization option menus fail via the \"Record the choice here.\" sentinel; parseDeck treats escaped pipes as literal cell characters and reports any non-5-cell row as malformed instead of silently skipping it. Six new fixtures pin the paths. Round 2: the grandfather phase reads the canonical project.phase path (product.phase never matched, so live apps would have gotten blocking errors; fixtures now build the canonical path instead of masking it); a missing TECH_SPEC.md with engineering done fails the externalization contract the same as an untouched option menu; business repos are scanned (app/, lib/, components/, src/ visible strings) for the fictional starter brands so a copied starter that never ran the copy pass fails app_copy.fictional_brand_shipped. Round 3: ONBOARDING screen-table rows whose cell count breaks the header are reported as malformed (a shifted cell hid copy from the scan), and mistyped backticked key references (onboarding.Promise.*) fail as malformed instead of silently matching nothing in coverage. Round 4: single-segment wildcard references (paywall.*) are retained in coverage instead of dropped by the dotted-token filter; the ONBOARDING placeholder scan reads backtick-stripped prose so a legitimate key like onboarding.email.placeholder never false-positives; a required deck must declare Status: authored (draft or unlabeled fails app_copy.deck_status_unauthored); the deck template advertises --state PROJECT_STATE.yaml since a relative state path joins the root. Round 5: every starter UI string is externalized through lib/strings.ts (login label/submit/confirmation, screen titles, chat placeholder/send/error, and the chat role labels that were rendering the raw user/assistant enum); the documented minimum surface set is enforced on required decks (app_copy.deck_surface_missing \u2014 rows or an explicit Not applicable line per canonical section); and a done design/onboarding lane requires an authored COPY_BRIEF.md (app_copy.brief_unauthored) so the deck never ships without its voice source."
  ]
}
