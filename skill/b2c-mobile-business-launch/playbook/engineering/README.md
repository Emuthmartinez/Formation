# Building The App

Architecture, data, auth, device testing, and the agent roles that carry the build.

Load the row whose trigger matches the work in front of you. Do not preload the set — each file is a full lane reference.

| Load when | Reference | Produces / gate |
| --- | --- | --- |
| before app handoffs, builder-ready bundles, business-repo `AGENTS.md`/`CLAUDE.md`, or post-launch operating docs | [`app-agent-roster.md`](app-agent-roster.md) | business-specific `AGENTS.md` and `CLAUDE.md` that keep future agents on this skill without another founder prompt, plus `APP_AGENTS.md` and seven role prompts under `agents/`: `orchestrator.md`, `marketing-guru.md`, `engineering-leader.md`, `product-leader.md`, `design-guru.md`, `customer-success.md`, `security-architect.md` · `check:agent-entrypoints` |
| before `engineering/TECH_SPEC.md` data/API sections harden; before archetype schema/auth prompts run; when the founder wants Firebase or a custom backend instead of the Supabase default; auditing an existing data layer | [`backend-data-contract.md`](backend-data-contract.md) | Backend Selection with a reason, Data Model, tested Authorization Model (RLS/security rules/middleware authz), Migrations And Environments; archetype prompts adapted to the chosen route rather than run verbatim · `check:backend-contract` |
| before app implementation, backend/frontend work, generated builder prompts, parallel agents, worktrees, `engineering/ENGINEERING_PLAN.md`, `engineering/PRODUCTION_READINESS.md`, or production-readiness claims | [`engineering-orchestration.md`](engineering-orchestration.md) | Route non-trivial work through CE freshness, `ce-brainstorm` when product shape is unresolved, `ce-plan`, `ce-work`/`ce-worktree`, `ce-code-review`, applicable CE test skills, and CE proof/demo routes. When CE is unavailable, record the fallback in state and run the Standalone Engineering Loop (`engineering-orchestration.md` §1b) at the same evidence bar — engineering stays partial until all five stages have evidence · `check:compound-engineering` |
| before MobAI device automation, `.mob` generation/healing, repeat or host-script actions, Sync Mode/multi-device work, performance gates, screenshots, polished demo videos, app previews, bug repro recordings, MobAI CI, or recorder-skill setup | [`mobai-toolbelt.md`](mobai-toolbelt.md) | `growth/DEMO_VIDEO.md` · `check:mobai-proof` |
| **first** for any "run the app", "check this screen", "walk this flow", or "reproduce this bug" request on a local Mac — the Route Ladder starts at the zero-setup in-app iOS Simulator (Claude Code Desktop pane, CLI `computer-use`, Codex `build-ios-apps`) before XcodeBuildMCP CLI/MCP. Also for Codex Desktop native iOS tools, SnapshotPreviews exports, serve-sim streaming, or simulator/device screenshots | [`xcodebuildmcp-testing.md`](xcodebuildmcp-testing.md) | Refresh official docs plus local `xcodebuildmcp --help`, tool lists, SnapshotPreviews README, and serve-sim README before writing commands or proof. In-app simulator screenshots leave the machine: **fixture accounts only, never a real founder/customer/provider login** · `check:native-ios` |
